import 'package:flutter/material.dart';

/// Deep navy hero tones — sit harmoniously beneath AppColors.primaryBlue.
/// Shared by every web layout (login, signup, and any future web screen).
const Color kHeroBgTop = Color(0xFF0B1B33);
const Color kHeroBgBottom = Color(0xFF060E1C);

/// At/above this width (web only) the two-panel hero layout is shown.
/// Below it, the web side collapses to a clean centered single column.
const double kWebTwoPanelMinWidth = 1000;
